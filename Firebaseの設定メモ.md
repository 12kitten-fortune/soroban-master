# Firebase の 設定メモ（先生画面）

先生画面（`docs/class/`）と 生徒の「教室に 参加」は、**設定が 無くても お試しモードで 動く**。
その端末の中だけで 完結するので、まず これで 感触を つかめる。
本番（別の端末から 先生が 見る）に するには 下の 4つ。

---

## ① Firebase プロジェクトを 作る

<https://console.firebase.google.com> →「プロジェクトを作成」
- 名前 `soroban-kingdom`
- **Gemini in Firebase：オフ**（要らない）
- **Google アナリティクス：オフ**（あとで 足せる）

## ② ログインの しくみを 有効にする

左メニュー「構築」→「**Authentication**」→「始める」

| 有効にするもの | 誰が つかう |
|---|---|
| **メール／パスワード** | 先生 |
| **匿名** | 生徒（端末を 教室に むすびつけるため） |

> 匿名を 忘れると、生徒が 参加できない。

## ③ Firestore を 作る

左メニュー「構築」→「**Firestore Database**」→「データベースを作成」
- 場所：**asia-northeast1（東京）**
- **本番環境モード**（守りは ④で 入れる）

作ったら「**ルール**」タブを ひらき、この リポジトリの **`firestore.rules`** を
まるごと 貼りつけて「**公開**」。

## ④ 設定を サイトに 貼る

左上の 歯車 →「プロジェクトの設定」→ 下の「マイアプリ」→ **`</>`（ウェブ）**
- ニックネーム `web`／**Hosting は チェックしない**

出てくる `firebaseConfig` の 中身を、**`docs/firebase-config.js`** の
`null` の ところに 貼る。

```js
window.SK_FIREBASE_CONFIG = {
  apiKey: "…", authDomain: "…", projectId: "…",
  storageBucket: "…", messagingSenderId: "…", appId: "…",
};
```

> `apiKey` は **秘密ではない**。ブラウザに 埋めこむ 前提の 識別子で、
> 守りは `firestore.rules` の 側で 行う。GitHub に 入れて よい。

最後に、コンソールの「Authentication → 設定 → 承認済みドメイン」に
**`sorobankingdom.com`** を 追加する（パスワード再設定メールの リンク先にも 使われる）。

## 先生の プランを 入れる（お金を いただいたら）

Stripe からの「新しい定期支払い」メールが 合図。全体の 流れは `Stripeの設定手順.md` の ⑦。

先生は 自分の プランを 書きかえられない（ルールで 禁止）。運営者が コンソールで 入れる。

1. コンソール → Firestore Database → `teachers` → その先生の 書類（uid。先生画面の メールで 探す）
2. 「フィールドを 追加」：
   - `plan`（文字列）：`class`（教室・40人）／`school`（スクール・150人）／`pioneer`（先行10教室・40人・1年 無料）
   - `planUntil`（文字列）：期限 `2027-09-12` の 形（先行教室は 1年後。有料は 空でも よい＝更新が 止まったら 手で `trial` に 戻す）
3. 先生が 画面を 開きなおすと 反映される（青い「いまの プラン」に 出る）

何も 入れていない 先生は `trial`（おためし：10人まで・登録から 31日）。期限を すぎると 生徒の 追加が できなくなる（記録は 見られる）。

> 2026-09-12：`teachers` の ルールが 変わった（plan / planUntil を 先生が 書けない）。**貼りなおして 公開**。

## ルールを 変えたとき（貼りなおしの 手順・5分）

`firestore.rules` を 変えたら、コンソールの「ルール」に **貼りなおして 公開** しないと 効かない
（サイトを push しても ルールは 変わらない）。

1. VS Code で、`soroban-master` の いちばん上に ある **`firestore.rules`** を ひらく（`docs` の 中では ない）
2. 中を クリック → **Ctrl+A**（ぜんぶ 選ぶ）→ **Ctrl+C**（コピー）
3. https://console.firebase.google.com/project/soroban-kingdom/firestore/rules を ひらく（Google で ログイン）
4. 真ん中の 編集画面を クリック → **Ctrl+A** → **Ctrl+V**（古いのを ぜんぶ 新しいのに 置きかえる）
5. 上の **「公開」** を おす。赤い エラーが 出なければ 完了
6. エラーが 出たら：いちばん上の `rules_version = '2';` から いちばん下の `}` まで 欠けずに 貼れているか 見る

> 2026-09-13：🏆 ランキング（`ranking/{月}/rows/{uid}`）を 足した → 貼りなおし 済み。
> 2026-09-13：家庭プランで `planMax` を 先生が 書けないように した → 貼りなおし 済み（2026-09-14）。
> 2026-09-15：Firestore 調査（A-2・A-3・A-4）の 見直し。**サイトを 先に push → 公開反映を 確かめてから ルールを 貼る**（順番が 逆だと、古い アプリの ランキング読み（300件）が 新ルール（100件まで）で 落ちる）。

## ⑤ App Check を 入れる（本物の サイトからの 通信だけ 受ける・無料）

apiKey は 公開前提なので、だれでも Firestore に 話しかけられる。App Check は「この サイトの 画面から 来た 通信」に 印を つけ、印の ない 通信を Firestore が はじく しくみ。ルールと 別の 守り。

1. Firebase コンソール → 左メニュー「構築」→「**App Check**」→「始める」
2. 「アプリ」タブ → ウェブアプリ `web` の 行 →「**reCAPTCHA v3**」を えらぶ
3. 別タブで <https://www.google.com/recaptcha/admin/create> を ひらき、reCAPTCHA v3 の キーを 作る
   - ラベル：`soroban-kingdom`
   - タイプ：**スコアベース（v3）**
   - ドメイン：`sorobankingdom.com`／`12kitten-fortune.github.io`／`localhost`（Cloudflare Pages に 移ったら その ドメインも 足す）
4. 出てきた「**サイトキー**」を Firebase の App Check の 画面に 貼って 保存（「シークレットキー」も 聞かれたら 貼る）
5. 同じ サイトキーを **`docs/firebase-config.js` の `appCheckKey`** に 貼って push
6. **まだ「適用」は 押さない。** 「API」タブ → Cloud Firestore の 行が「未適用（モニタリング）」の まま 2〜3日 待つ。
   グラフの「検証済み」が ほぼ 100% に なっているのを 見てから「**適用**」を 押す
   （先に 適用すると、古い アプリを ひらいている 子の 通信が 全部 落ちる）
7. 手元の パソコンで 本番モードを 試すときは URL に `?appcheckdebug=1` を つける → F12 の コンソールに 出る
   「App Check debug token」を、App Check → アプリ → `web` の ⋮ →「デバッグトークンを 管理」に 登録

## ⑥ API キーに「使える サイト」の 制限を つける

1. <https://console.cloud.google.com/apis/credentials?project=soroban-kingdom> を ひらく
2. 「API キー」の「**Browser key (auto created by Firebase)**」を 押す
3. 「アプリケーションの制限」→「**ウェブサイト**（HTTP リファラー）」→ 次を 1行ずつ 足す
   - `https://sorobankingdom.com/*`
   - `https://www.sorobankingdom.com/*`
   - `https://12kitten-fortune.github.io/*`
   - `https://soroban-kingdom.firebaseapp.com/*`（パスワード再設定の ページが 使う。**消さない**）
   - Cloudflare Pages に 移ったら その ドメイン（例 `https://xxxx.pages.dev/*`）
4. 「API の制限」は **「キーを 制限しない」の まま**（しぼりすぎると ログインや 再設定メールが 止まる。今回は さわらない）
5. 「保存」。反映まで 5分ほど。反映後に、先生画面の ログインと「パスワードを 忘れた」の メールが 届くかを 1回 試す

> リファラー制限は「うっかり 他所で 使われる」のを 防ぐ 程度（偽装は できる）。本命は ⑤ の App Check。

## ⑦ Blaze（従量課金）に 切りかえる ＋ 予算アラート

理由：Spark は 1日の 上限に 当たると **その日は 全教室が 止まる**（他人の 操作でも 起きうる）。Blaze は 止まらず、無料枠は 同じ。削減後の 見込みは 月 0〜200円。

1. Firebase コンソール 左下の「**Spark**」→「**アップグレード**」→ Blaze を えらぶ
2. Cloud 請求先アカウントを 作る（カード登録・国＝日本・通貨＝JPY）
3. 同じ 画面で「予算額」を 聞かれたら **500** 円 と 入れる（あとから ⑦-2 でも 設定できる）
4. 「購入」→ 数秒で 切りかわる（サイトは 止まらない）

### ⑦-2 予算アラート（切りかえ 直後に）

1. <https://console.cloud.google.com/billing> →「**予算とアラート**」→「**予算を 作成**」
2. 名前 `soroban-kingdom`／プロジェクト＝`soroban-kingdom`／期間＝月次
3. 金額＝「指定額」**500** 円
4. しきい値＝**50%・90%・100%**、「予測」の 100% にも チェック → メールで 通知
5. 「完了」。※アラートは **通知だけ**。課金は 自動で 止まらない（止めたいときは 請求先アカウントの 紐づけを 外す）

## ルールを 貼ったあとの 確認（記録が ちゃんと 保存されるか・10分）

準備：サイトの push が 反映されているか（生徒アプリを **Ctrl+Shift+R**、先生画面も 同じ）。それから ルールを 貼って 公開。

1. **生徒の 端末**（教室に 参加ずみ）で 練習を 1セット おわらせる → 5秒 待つ
   - 「教室」の 画面の 下に「✓ 1件 とどきました」と 出る
   - Firestore データ：`classes/教室/students/その子/sessions` に 今の 時刻の 書類が 1つ 増えている
   - その子の 書類の `lastSeen` が 今の 時刻、`stat.n7` が 1 増えている
2. **先生画面**を ひらきなおす → 生徒の 表の「直近7日」の 数が 増えている
3. 先生画面で **宿題を 1つ 出す** → Firestore データ：`classes/教室` の `hw` 配列に 1つ 増えている（`homework` の 下では ない）
   → 生徒の アプリを ひらきなおす → ホームに その 宿題が 出る → 消す
4. 生徒の アプリの 🏆 ランキング（参加中の 端末）を ひらく → 自分の 行が 今月の 数で 出る。国内・教室の タブも ひらける
   （初回に F12 に「索引が ありません」と 出たら、その 下の リンクを 押して 索引を 作る。10分ほどで 効く）
5. **別の 端末**（または ブラウザの シークレット窓）で クラスコード → 名前 → 参加 →「参加したよ！」が 出る
6. F12 → コンソールに `permission-denied` が 出ていないこと

もし 落ちたら：コンソールの「ルール」で 1つ前の 版に 戻せる（「ルール」画面の 左の 履歴から 選んで「公開」）。

---

## データの 形

```
teachers/{uid}                              先生の 名前
codes/{コード6文字}   → { classId }          コード → 教室
classes/{cid}                               教室（name, code, teacherUid, preset, curriculum）
                                            preset＝級の基準（sk／sk10／custom）。custom のとき curriculum に この教室だけの 級の表（先生画面で 直す。子どもは 参加時と 起動時に 受けとる。ルールでは 検査していない＝子ども側で 形を 検査して こわれていたら 標準）
  students/{sid}                            生徒（nick, uids[], stat）
    sessions/{時刻}                          1セットの 記録
  homework/{hid}                            先生が 出した 宿題（subj, g, sets, due, note, createdAt）
                                            できた数は 生徒側が 数えて stat.hw に 入れる
```

> 2026-09-11：宿題を 足した。`firestore.rules` に `homework` の 守りが 増えているので、
> コンソールの「ルール」に **貼りなおして 公開** すること（貼らないと 先生が 宿題を 出せない）。

**生徒からは 集めないもの**：メール・本名・生年月日・住所。
にっくねーむと 練習の 記録だけ。

## 守りの 考え方（firestore.rules）

- 先生（メール＋パスワード）＝ **自分の教室だけ** 読み書き
- 生徒（匿名）＝ 教室の **名前の一覧は 見られる**が、
  ほかの子の **記録は 読めない**。書けるのは **自分の 記録だけ**、しかも **足すだけ**（消せない・書きかえられない）
- 生徒は 教室や ほかの生徒を **作れない・消せない**

## 無料枠

Firestore の 無料枠は 1日 読み 5万 / 書き 2万。
30人 × 10教室が 毎日 4セットでも 1日 1,200書き。**十分 収まる。**

## 確かめ方

```
node class-test.mjs        （scratchpad。先生→生徒→先生 の 通し確認）
```
お試しモードでの 通しなので、Firebase を 入れたあとは
実際に 2つの 端末（先生＝パソコン／生徒＝スマホ）で 一度 試すこと。
