# そろばんキングダム — バグ修正と品質改善の依頼

あなたは、子ども向け学習アプリを**世に出せる品質まで仕上げる**エンジニアです。
このリポジトリのバグを**実際に直して**ください。報告だけでなく、**コードを修正**してください。

これは近く**有料で販売する**アプリです。お金を受け取る以上、
「たまに壊れる」「気づかず動いていない」は許されません。

---

## 1. これは何のアプリか

日本の子ども（6〜12歳）向けの**そろばん・暗算の練習アプリ**。
公開先は GitHub Pages（`https://12kitten-fortune.github.io/soroban-master/`）。

| 画面 | 中身 |
|---|---|
| ホーム | 今日の練習メニュー、GOLD、連続学習日数 |
| 級・段を選ぶ | 20級〜十段 × みとり算／かけ算／わり算／フラッシュ暗算／あんざん |
| れんしゅう | 画面上のそろばん（15桁）を指で弾いて答える。◎×判定、まちがい方の図解解説 |
| 本日の練習 | 複数セット＋休憩を自動で回し、最後に成績発表 |
| たいせん | 制限時間内に敵を倒すバトル形式 |
| パズル | ロイヤルマッチ風の 8×8 マッチ3。GOLD を払って遊ぶ |
| 記録を見る | セッション履歴、週間ランキング、実力ランク |
| プレミアム | 有料プラン（買い切り1,500円）とコード入力 |

ねらいは「**そろばん自体に夢中にさせる**」こと。パズルやバトルは入口で、
本体は「自分の**まちがえ方のクセ**が図解で見える」ところにあります。

---

## 2. 守ること（**ここは変えないでください**）

- **ビルド工程なし・フレームワークなし。** 素の HTML / CSS / JavaScript のみ。
  `npm install` した物を**動作に必要にしないでください**（そのまま GitHub Pages に置いて動くこと）。
- **サーバーなし。** 静的ファイルのみ。データは `localStorage`。
- ファイル構成は変えない：
  - `docs/app.js` … 約4,700行。**全機能がここに入っている**
  - `docs/index.html` … 全画面が1ファイルに入っていて `.hidden` で切り替え
  - `docs/style.css`
  - `docs/sw.js` … Service Worker（network-first）
- コメントは**日本語**、変数名は英語。この方針を維持。
- 更新のたびに `docs/app.js` の `BUILD` と `docs/sw.js` の `CACHE` を上げる運用。
- 検定基準の数値（`SOROBAN_STD` / `ANZAN_STD`）は**変えないでください**。
  日本計算技能連盟の公開サンプルから採った実測値です。
- 見た目の方針（王国・レオ王・金と紺）は維持。

### 変えてよいもの（前回から方針が変わりました）

- **`localStorage` のキー名・データ形式は自由に変えてよい。**
  これまでの子どもの使用記録は**消えて構いません**。
  ただし、作り直すなら `soroban_ver` のような版のキーを入れて、
  版が違えば**一度だけきれいに消す**仕組みを必ず付けてください。
  「古い形のデータが残っていて表示が壊れる」状態を作らないこと。
- 消し残しの削除、関数の整理、CSSの整理は歓迎します。

---

## 3. 直してほしいこと（優先順）

### 最優先 ①「**実行されると必ず落ちる**が、誰も気づいていない」箇所を全部つぶす

つい先日、こういうバグが**本番で動いていました**：

```js
// finishRoutine() の中
goldLines.push("🏁 やりきった ＋" + routineBonus);   // ← ここで参照
const routineBonus = Math.round(60 * gradeGoldMult(rs.grade));  // ← 宣言はこの後
```

`const` の**一時的死角（TDZ）**による `ReferenceError`。
本日の練習を終えた瞬間に例外で処理が止まり、
**成績発表・まちがいの解説・GOLD が まるごと出ていませんでした**。
利用者に「何も出ない」と言われるまで、誰も気づきませんでした。
同様に、廃止した機能の関数を呼び続けて `resizeCraftCanvas is not defined` を
出していた箇所もありました。

**同じ種類の地雷が他にも埋まっているはずです。全部見つけて直してください。**

- 宣言前参照（TDZ）、未定義の関数・変数の呼び出し
- `null` になりうる DOM 参照（`$("#x").classList` の形）
- `catch {}` で握りつぶしていて、**失敗が誰にも見えない**箇所（`catch` は約70か所）
  → 「隠してよい失敗（音が鳴らない等）」と「隠してはいけない失敗（採点・保存・課金）」を
    分け、後者は**画面に出す**か、少なくとも `console.error` に残してください
- **めったに通らない分岐**：検定モード、休憩スキップ、一時停止からの中断、
  タイムアップ、段位、フラッシュ暗算の検定モード、たいせんの敗北、
  パズルの手数ぎれ、GOLD不足、初回起動（データが空）、日付をまたいだとき

**探し方の提案**：主要な画面遷移と、上に挙げた分岐を実際に通して
`window.onerror` を拾うスクリプトを書けば、機械的に見つかります。

### 優先 ② 有料プランの**抜け道**をふさぐ

`gradeOpen(g)` が「9級以上はプレミアム」を判定します。
現在チェックしているのは `startSession` / `startRoutine` / `startBtn` / `updateInfo` だけ。

- **`startBattle()`（`docs/app.js:2334` 付近）は `gradeOpen` を見ていません。**
  たいせん画面から級を選べば、9級以上が無料で遊べます。**これは必ず直してください。**
- ほかにも迂回できる経路（キーボード操作、`#againBtn`、`startWeakSession`、
  `startFlash` の直接呼び出しなど）を**全部**洗い出してふさいでください
- ※ブラウザ内だけの判定なので開発者ツールを使えば破れるのは**承知の上**。
  そこは対応不要。**普通の操作で意図せず通ってしまう穴**だけを直してください
- 逆に、**払った人が使えない**バグ（コードを入れたのに反映されない等）も潰してください。
  こちらのほうが重大です

### 優先 ③ 非同期処理の競合

パズルは `async/await` でアニメーションを回します（`pzTry` / `pzUseTool` /
`pzCascadeAnim` / `pzPlayBlast`）。多重実行は `pz.busy` フラグ1つで防いでいます。

- `await` 明けに `pz` が `null` / 別レベルに差し替わっている経路
- `pz.busy` が立ったまま戻らなくなる経路
  （過去に「盤が固まって終われない」不具合あり。`try/finally` で対処済みだが漏れがないか）
- `setTimeout` で仕込んだ処理が、画面遷移後に**古い状態を触らないか**
- 練習・たいせんのタイマー（`playTimer` / `battleTimer` / `restTimer`）が
  **止め忘れで二重に走る**経路

### 優先 ④ iPhone / iPad での実使用（**縦向きの iPhone が最重要**）

- 練習中はそろばんを画面下に `position: fixed` で固定
  （`body.playing #playSorobanWrap`）。iOS Safari の**下バーの伸縮**で
  高さが変わる場面（`visualViewport`）の破綻
- そろばんは横スクロールします（15桁が画面に入らないため）。
  珠を弾くドラッグと横スクロールの**取り合い**
- **音声が 17MB**（BGM 1曲 2.6〜3.2MB）。モバイル回線での初回読み込みが重い。
  遅延読み込み・必要な曲だけ読む・ビットレートを落とす等、**軽くする提案と実装**を
- Service Worker が network-first。**電波が弱いとき**に真っ白にならないか

### 優先 ⑤ 子ども向けとしての質

- 文言が**小学校低学年に読めるか**（漢字・言い回し）
- 失敗したときに**責めていないか**
- 有料の案内が**しつこすぎないか**（子どもが見る画面です）
- 触れる的の大きさ（44px以上）、コントラスト、`aria-*`、キーボード操作

### 優先 ⑥ 消し残しの削除

「クラフト」機能は**廃止した**のに、`docs/index.html` の `#view-craft` と
`docs/app.js` の craft 関連（約140か所）、`style.css` の該当部分が残っています。
**きれいに削除してください。** 保存データの互換性は気にしなくて構いません（§2参照）。

---

## 4. すでに分かっていること（**重複して報告しないでください**）

- コード認証がブラウザ内だけなので破れる → 承知の上。将来 Cloudflare Workers に移す想定
- `app.js` が1ファイル4,700行 → **分割の提案は不要**（ビルド工程を持たない方針）
- テストが本体ソースを文字列で切り出して実行している → 承知の上
- 記録は端末ごと（アカウントなし）→ 承知の上
- `docs/assets/*.png` は生成AI製、BGMは魔王魂、効果音は効果音ラボ → クレジット表記済み

---

## 5. 作業のしかたと、終わったあとの報告

### 進め方

1. まず**壊れている所を機械的に洗い出す**（§3① の探し方）
2. **重大なものから直す**。1つ直すごとに `node --check docs/app.js` を通す
3. 直したら**実際に動かして確かめる**。とくに以下は必ず最後まで通すこと：
   - **本日の練習**（20級）を最後まで解いて、成績発表・解説・GOLD が出ること
   - れんしゅう（5級 みとり算）を1セット完走
   - たいせんを1回、パズルを1面クリアと1面失敗
   - プレミアムのコードを入れて、9級以上が開くこと
4. `BUILD` と `CACHE` を上げる

### 確認コマンド

```bash
node --check docs/app.js          # 構文
npx serve docs                    # ローカルで開く（任意の静的サーバーで可）
```

### 報告の形

**直したもの**を、深刻な順に：

```
### [重大 / 中 / 軽微] 一行でまとめた見出し
- **場所**: docs/app.js:1234（関数名）
- **どう壊れていたか**: どんな操作をすると、利用者に何が起きていたか
- **原因**: なぜそうなったか
- **直し方**: 何をどう変えたか
- **確かめ方**: どう動かして直ったと判断したか
```

続けて、**直さなかったが気づいたこと**を同じ形で。理由も添えてください。

- 「重大」は**利用者の操作が実際に壊れる／データが消える／お金が絡む**もの限定
- **推測ではなく、コードの根拠を示してください。** 行番号は必須
- 実際に起きる操作手順が書けないものは「軽微」に落としてください
- 良かった点の列挙は不要です
