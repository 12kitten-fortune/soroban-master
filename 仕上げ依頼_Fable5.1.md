# そろばんキングダム — 「世に出せる品質」に仕上げてください

> このファイルは `修正依頼_Fable5.1.md` を**置きかえるもの**です。そちらは読まなくて構いません。

あなたは、子ども向け学習アプリを**製品として出荷できる状態まで仕上げる**エンジニアです。
**報告だけでなく、実際にコードを直してください。**

このアプリは近く**有料で販売します**（買い切り1,500円）。
お金を受け取る以上、「たまに壊れる」「気づかず動いていない」「作りかけ」は許されません。

---

## 0. 「仕上がった」の合格ライン

**次が全部○になったら完成です。** 曖昧な「よくなった」ではなく、この表で判定してください。

| # | 合格ライン | 確かめ方 |
|---|---|---|
| 1 | どの画面をどう操作しても、**コンソールにエラーが1つも出ない** | 「§5 通し確認」を全部やって DevTools を見る |
| 2 | **本日の練習**を最後まで通すと、成績発表・まちがいの解説・GOLD が必ず出る | 20級と5級で各1回 |
| 3 | 有料の級（9級以上）に、**普通の操作でたどり着ける道が1つもない** | §3-A② の経路を全部試す |
| 4 | コードを入れた人が、**必ず全部使える** | コードを入れて全画面を回る |
| 5 | **はじめて開いた子ども**が、説明なしで最初の1問にたどり着ける | データを全部消して開く |
| 6 | 廃止した機能の**残骸が1行もない** | `grep -ri craft docs/` が 0 件 |
| 7 | **使っていないファイルが1つもない** | 画像・音・CSSクラス・関数 |
| 8 | iPhone（390×844）縦で、**全画面が横にはみ出さない** | `documentElement.scrollWidth <= clientWidth` |
| 9 | 全画面で、**文字が小学2年生に読める** | 漢字・言い回しを1画面ずつ見る |
| 10 | `node --check docs/app.js` が通る | 毎回 |

---

## 1. これは何のアプリか

日本の子ども（6〜12歳）向けの**そろばん・暗算の練習アプリ**。
公開先は GitHub Pages（`https://12kitten-fortune.github.io/soroban-master/`）。

| 画面 | 中身 |
|---|---|
| ホーム | 今日の練習メニュー、GOLD、連続学習日数 |
| 級・段を選ぶ | 20級〜十段 × みとり算／かけ算／わり算／フラッシュ暗算／あんざん |
| れんしゅう | 画面上のそろばん（15桁）を指で弾いて答える。◎×判定、まちがい方の図解解説 |
| 本日の練習 | 複数セット＋休憩を自動で回し、最後に成績発表 |
| たいせん | 制限時間内に敵を倒すバトル形式 |
| パズル | ロイヤルマッチ風の 8×8 マッチ3。GOLD を払って遊ぶ |
| 記録を見る | セッション履歴、週間ランキング、実力ランク |
| 保護者 | 週ごとの学習量・得意苦手 |
| 検定内容・解き方 | そろばんの基礎（いまはテキストのみ） |
| 設定 | 名前・アバター・音 |
| プレミアム | 有料プラン（買い切り1,500円）とコード入力 |

**このアプリの本体は「まちがえ方のクセが図解で見える」ところ**です。
パズルやバトルは、そこへ連れてくるための入口にすぎません。
**仕上げるときは、この優先順位を守ってください。**

---

## 2. 守ること（**ここは変えないでください**）

- **ビルド工程なし・フレームワークなし。** 素の HTML / CSS / JavaScript のみ。
  `npm install` した物を**動作に必要にしないでください**（そのまま GitHub Pages に置いて動くこと）。
- **サーバーなし。** 静的ファイルのみ。データは `localStorage`。
- ファイル構成は変えない：`docs/app.js`（約4,700行・全機能）／`docs/index.html`（全画面が1ファイル、`.hidden` で切替）／`docs/style.css`／`docs/sw.js`
- コメントは**日本語**、変数名は英語。
- 更新のたびに `docs/app.js` の `BUILD` と `docs/sw.js` の `CACHE` を上げる。
- 検定基準の数値（`SOROBAN_STD` / `ANZAN_STD`）は**変えない**。
  日本計算技能連盟の公開サンプルから採った実測値です。
- 見た目の方針（王国・レオ王・金と紺）は維持。
- 素材のクレジット表記（魔王魂・効果音ラボ・生成AI）を**消さない**。魔王魂は表記が義務です。

### 変えてよいもの

- **`localStorage` のキー名・データ形式は自由に変えてよい。**
  これまでの使用記録は**消えて構いません**。
  ただし `soroban_ver` のような版のキーを入れ、**版が違えば一度だけきれいに消す**
  仕組みを必ず付けてください。「古い形のデータが残って表示が壊れる」状態を作らないこと。
- 関数の整理、CSS の整理、未使用ファイルの削除は歓迎します。

---

## 3. やってほしいこと

### A. 壊れているものを直す（最優先）

#### ①「実行されると必ず落ちる」が誰も気づいていない箇所を、全部つぶす

つい先日、こういうバグが**本番で動いていました**。

```js
// finishRoutine() の中
goldLines.push("やりきった +" + routineBonus);   // ← ここで参照
const routineBonus = Math.round(60 * gradeGoldMult(rs.grade));  // ← 宣言はこの後
```

`const` の**一時的死角（TDZ）**による `ReferenceError`。本日の練習を終えた瞬間に
処理が止まり、**成績発表・解説・GOLD がまるごと出ていませんでした**。
利用者に「何も出ない」と言われるまで、誰も気づきませんでした。
廃止した機能の関数を呼び続けて `resizeCraftCanvas is not defined` を
出していた箇所もありました。

**同じ種類の地雷が他にも埋まっているはずです。全部見つけて直してください。**

- 宣言前参照（TDZ）、未定義の関数・変数の呼び出し
- `null` になりうる DOM 参照（`$("#x").classList` の形）
- `catch {}` で握りつぶしていて**失敗が誰にも見えない**箇所（約70か所）
  → 「隠してよい失敗（音が鳴らない等）」と「隠してはいけない失敗（採点・保存・課金）」を
  分け、後者は画面に出すか、最低でも `console.error` に残す
- **めったに通らない分岐**：検定モード／休憩スキップ／一時停止からの中断／
  タイムアップ／段位／フラッシュ暗算の検定モード／たいせんの敗北／
  パズルの手数ぎれ／GOLD不足／**初回起動（データが空）**／日付をまたいだとき

**探し方の提案**：主要な画面遷移と上の分岐を機械的に通し、`window.onerror` を
拾うスクリプトを書けば、まとめて見つかります。

#### ② 有料プランの抜け道をふさぐ

`gradeOpen(g)` が「9級以上はプレミアム」を判定します。
現在チェックしているのは `startSession` / `startRoutine` / `startBtn` / `updateInfo` だけ。

- **`startBattle()`（`docs/app.js:2334` 付近）は `gradeOpen` を見ていません。**
  たいせん画面から級を選べば、9級以上が無料で遊べます。**必ず直してください。**
- ほかの迂回路（キーボード操作、`#againBtn`、`startWeakSession`、`startFlash` の
  直接呼び出しなど）も**全部**洗い出してふさぐ
- ※開発者ツールを使えば破れるのは**承知の上**。対応不要。
  **普通の操作で意図せず通ってしまう穴**だけを直してください
- **逆方向のバグ（払った人が使えない）のほうが重大です。** 必ず潰してください

#### ③ 非同期処理の競合

パズルは `async/await` でアニメーションを回します（`pzTry` / `pzUseTool` /
`pzCascadeAnim` / `pzPlayBlast`）。多重実行は `pz.busy` フラグ1つで防いでいます。

- `await` 明けに `pz` が `null` / 別レベルに差し替わっている経路
- `pz.busy` が立ったまま戻らない経路（過去に「盤が固まって終われない」不具合あり）
- `setTimeout` で仕込んだ処理が、画面遷移後に**古い状態を触らないか**
- タイマー（`playTimer` / `battleTimer` / `restTimer`）の**止め忘れによる二重起動**

---

### B. 作りかけを仕上げる

**調査済みです。以下が未完成のまま残っています。**

#### ① はじめて開いた子どもへの案内が、ほぼ無い

`tipOnce()` という「1回だけ出る説明」の仕組みがあるのに、
**パズルの草・木箱でしか使われていません**（`docs/app.js:4531, 4537`）。

データを全部消して開いた子どもは、**そろばんの珠の動かし方すら教わりません。**
少なくとも次を、絵か図つきの `tipOnce` で出してください。

- はじめての起動：このアプリで何をするのか（3画面以内）
- はじめての練習：**珠は指でなぞって動かす**こと、赤い点が一の位であること
- はじめての「本日の練習」：セットと休憩があること
- はじめての成績発表：まちがいの解説がここに出ること
- はじめてのパズル：GOLD を使うこと

#### ② 入門級（20〜15級）の「絵や図での説明」が無い

「検定内容・解き方」画面（`#view-lesson`）は**大人向けのテキストだけ**です。
`ANZAN_LOW`（`docs/app.js:57`）で 20〜11級に
「こたえが5まで」「五玉をつかう」「10の友」といった**学習の順序**が
定義されているのに、**その順序を子どもに絵で説明する画面がありません。**

そろばんの習得順（**5の友 → くり上がりなし → 10の友 → くり上がり**）に沿って、
**その級を始める前に、珠の絵で説明する**ようにしてください。
絵は画像ファイルを足さず、**CSS か インライン SVG で描いてください**
（既存のそろばん描画のしくみを流用できます）。

#### ③ パズルの ★ に使い道が無い

`pzStars()` で ★ を記録し（`docs/app.js:3973`）、過去のレベルの横に表示するだけです
（`docs/app.js:4487`）。**集めても何も起きません。**
★ が貯まると何か起きるようにしてください（例：★n個でアイテム、王国の飾りが増える等）。
**ただし GOLD の経済を壊さないこと**（GOLD はそろばんの練習でしか増えない設計です）。

#### ④ 空のときの表示

各画面に「まだ記録がありません」系の文言が11か所ありますが、
**全画面ぶんは揃っていません。** データが空の状態で全画面を開き、
**何も出ない／崩れる画面が1つも無い**ようにしてください。

---

### C. 消し残しを消す

「クラフト」機能は**廃止済み**なのに、残骸があります。

- `docs/index.html` の `#view-craft`（264行目付近）
- `docs/app.js` の craft 関連 **約140か所**
- `docs/style.css` の該当部分
- **使っていない画像**：`castle.png` / `house_brown.png` / `house_green.png` /
  `king_run.png` / `pz_orb.png`
- 使われていない `localStorage` キー：`soroban_town` / `soroban_craft`

**全部きれいに消してください。** 互換性は気にしなくて構いません（§2参照）。
消したあと `grep -ri craft docs/` が 0 件になること。

---

### D. iPhone / iPad での実使用（**縦向きの iPhone が最重要**）

- 練習中はそろばんを画面下に `position: fixed` で固定（`body.playing #playSorobanWrap`）。
  iOS Safari の**下バーの伸縮**で高さが変わる場面（`visualViewport`）の破綻
- そろばんは横スクロールします（15桁が画面に入らないため）。
  珠を弾くドラッグと横スクロールの**取り合い**
- **音声が 17MB**（BGM 1曲 2.6〜3.2MB）。モバイル回線での初回が重い。
  遅延読み込み・必要な曲だけ読む・ビットレートを落とす等、**軽くする実装**を
- Service Worker が network-first。**電波が弱いとき**に真っ白にならないか

---

### E. 子ども向けとしての質

- 文言が**小学2年生に読めるか**（漢字・言い回し）を1画面ずつ点検
- 失敗したときに**責めていないか**
- 有料の案内が**しつこすぎないか**（子どもが見る画面です。1画面に1回まで）
- 触れる的の大きさ（44px以上）、コントラスト、`aria-*`、キーボード操作
- **画面ごとに言葉づかいがばらついていないか**（「もどる」「戻る」の混在など）

---

## 4. すでに分かっていること（**重複して報告しないでください**）

- コード認証がブラウザ内だけなので破れる → 承知の上。将来 Cloudflare Workers に移す想定
- `app.js` が1ファイル4,700行 → **分割の提案は不要**（ビルド工程を持たない方針）
- テストが本体ソースを文字列で切り出して実行している → 承知の上
- 記録は端末ごと（アカウントなし）→ 承知の上
- 素材：BGM は魔王魂（クレジット必須）／効果音は効果音ラボ／絵は ChatGPT 生成。**確認済み**
- 世界ランキングが無い → サーバーが要るため、今回の範囲外

---

## 5. 通し確認（**必ず全部やってください**）

DevTools のコンソールを開いたまま、**エラーが1つも出ないこと**を確認しながら。

1. **`localStorage` を全部消して**開く（初回の子どもの状態）
2. ホーム → 本日の練習（20級）を**最後まで**解く。成績発表・解説・GOLD が出るか
3. 級・段を選ぶ → 5級 みとり算を1セット完走（検定モード ON／OFF 両方）
4. フラッシュ暗算を1セット（検定モードも）
5. たいせんを1回（勝ちと負けの両方）
6. パズルを1面クリア、1面失敗、道具を1つ使う、手数ぎれで「+5手つづける」
7. 記録を見る／保護者／設定 を開く
8. プレミアム画面でコードを入れ、9級以上が開くこと
9. **もう一度 `localStorage` を消して**、上を iPhone 表示（390×844）でやり直す
10. `node --check docs/app.js`

```bash
npx serve docs      # 任意の静的サーバーで可
```

---

## 6. 報告の形

**直したもの**を、深刻な順に。

```
### [重大 / 中 / 軽微] 一行でまとめた見出し
- 場所: docs/app.js:1234（関数名）
- どう壊れていたか: どんな操作をすると、利用者に何が起きていたか
- 原因: なぜそうなったか
- 直し方: 何をどう変えたか
- 確かめ方: どう動かして直ったと判断したか
```

最後に、**§0 の合格ライン10項目**を1つずつ ○/× と根拠つきで報告してください。
× が残る場合は、その理由も。

- 「重大」は**利用者の操作が実際に壊れる／データが消える／お金が絡む**もの限定
- **推測ではなく、コードの根拠を示してください。** 行番号は必須
- 実際に起きる操作手順が書けないものは「軽微」に落とす
- **良かった点の列挙は不要です**
